READ_ME
panel_comparator.py 

input - tab delimited text file with data in columns. Each column must have a header.
output - python dictionary { 'column_a vs column_b' : [[unique to column_a], [unique to column_b], [shared in both columns]]}

Compares the contents of two columns in a text file.
the example "__main__" statement used does the following:
 - initialises column file and provides the path to the input file 'master_panels.txt'
 - calls the function Column_file.load() to initialises a Column_as_list object for each column and appends it to the column_objects array 
 - compares the contents of the user specified columns using Column_file.compare()
 - prints the comparison to screen
